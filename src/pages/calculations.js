import React from "react";
import { Link } from "gatsby";

import Layout from "../components/layout";
import SEO from "../components/seo";

const Gallery = () => (
  <Layout>
    <SEO title="Calculations" />
    <main>
        <h1>Calculations</h1>
    </main>
  </Layout>
);

export default Gallery;
